#include <stdio.h>
/**
 * main - prints "with proper grammar, but the outcome is a biece of art,"
 followed newline
 * Return:0;
*/
int main(void)
{
        printf("Print with proper grammar, but the outcome is a biece of art,\n");
        return (0);
}
